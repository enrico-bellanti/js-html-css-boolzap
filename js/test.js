

$(document).ready(function() {
  // al digitare qualcosa nella barra input_search

// end document ready
});


// importa il valore da unread_message e incrementalo di 1 unita
// poi riscrivi il valore nel unread_message
// var unreadCounter = parseInt(unreadMessage.text());
// console.log(unreadCounter);
// unreadCounter = unreadCounter + 1;
// console.log(unreadCounter);
// unreadMessage.text(unreadCounter);
